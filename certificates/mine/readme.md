

# Certificates of this instance
- only one keypair should reasonably exist, used in both server and client configuration
- share public key (*.key) with whoever should accept connections from this instance
- do not share private (*.key_secret)
